---
description: Compare brute force vs optimized with side-by-side 3D visualizations
argument-hint: "<pattern> <problem>"
---
Compare brute force vs optimized $1 for: ${2:-the classic problem}

Generate TWO separate 3D HTML files:
1. `<problem>-brute-force-3d.html` — the naive approach showing all recomputed work in red
2. `<problem>-optimized-3d.html` — the $1 approach showing saved work in green

Then explain the trade-off: what we gained (time), what we paid (space/constraints).

Follow dsa-visual-teacher template. Follow i-have-adhd output rules.